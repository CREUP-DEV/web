// Newsletter upload size limits, shared between the admin client form and the server upload route.

/** Max cover image size (5 MB). */
export const NEWSLETTER_MAX_IMAGE_SIZE = 5 * 1024 * 1024

/** Max newsletter PDF size (20 MB). */
export const NEWSLETTER_MAX_PDF_SIZE = 20 * 1024 * 1024
